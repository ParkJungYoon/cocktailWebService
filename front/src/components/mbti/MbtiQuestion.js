import { Typography } from "@mui/material";
import React from "react";

const MbtiQuestion = (props) => {
  return <Typography variant="h5">{props.question}</Typography>;
};

export default MbtiQuestion;
